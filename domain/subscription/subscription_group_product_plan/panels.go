package subscription_group_product_plan

// S4 class-detail tab panel ids — the HTMXSuccess refresh targets shared by
// action/ (assign, set-status, picker) and detail/ (the tab markup itself),
// kept here as the single source of truth so the two sub-packages can never
// drift on the literal string.
const (
	InfoTabPanelID  = "sgpp-info-tab"
	StaffTabPanelID = "sgpp-staff-tab"
)
